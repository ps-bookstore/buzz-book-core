spring:
  elasticsearch:
    uris: https://43.202.250.117:9200
    username: elastic
    password: ${secrets.ELA_PASSWORD}

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://43.202.250.117:3306/be6_aa_prod?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: ps
    password: ${secrets.DB_MQ_PASSWORD}

  rabbitmq:
    host: 43.202.250.117
    port: 5672
    username: ps
    password: ${secrets.DB_MQ_PASSWORD}

    # multipartFile 용량 늘려주는 설정

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

cloud:
  aws:
    credentials:
      access-key: ${secrets.S3_ACCESS_KEY}
      secret-key: ${secrets.S3_SECRET_KEY}
    region:
      static: ap-northeast-2
    stack:
      auto: false
